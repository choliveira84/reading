spring:
    application:
        name: ms-cadastro

    cloud:
        config:
            uri: http://reading-config-server:8888

    profiles:
        active: prod

# management:
#     endpoints:
#         web:
#             exposure:
#                 include: *