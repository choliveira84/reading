server:
    port: 10345
    address: localhost
  
spring:
    profiles:
        active: test
    application:
        name: ms-notificacao
    datasource:
        driver-class-name: org.h2.Driver
        url: jdbc:h2:mem:msnotificacao;MODE=MYSQL;DB_CLOSE_DELAY=-1
        username: sa
        password: 
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate:
            ddl-auto: create
