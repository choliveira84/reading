spring:
    application:
        name: ms-notificacao

    cloud:
        config:
            uri: http://config-server:8888

    profiles:
        active: prod

# management:
#     endpoints:
#         web:
#             exposure:
#                 include: *