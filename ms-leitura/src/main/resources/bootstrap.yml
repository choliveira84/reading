spring:
    application:
        name: ms-leitura

    cloud:
        config:
            uri: http://localhost:8888

    profiles:
        active: prod

# management:
#     endpoints:
#         web:
#             exposure:
#                 include: *